import { HeroContainer } from "../../styles/";
import Nav from "./Nav";

const Hero = (): JSX.Element => {
	return (
		<HeroContainer>
			<Nav />
		</HeroContainer>
	);
};

export default Hero;
